.col-md-10.center-block.no-float
	%article.card.large-padding
		%h1 Tu pago ha sido procesado
		%p Hemos enviado a tu correo los links de descarga del producto que acabas de adquirir
		.top-space
		.top-space
			%h4.red-text Titlantli (Esto es lo que se te mando)
			%table.table.table-striped.table-hover.medium#table_shopping_cart
				%thead
					%td Producto
					%td Costo
				%tbody
					-@shopping_cart.in_shopping_cart.each do |i_sh|
						-product = i_sh.product
						%tr{id: "#{product.id}"}
							%td= product.name
							%td= product.pricing
